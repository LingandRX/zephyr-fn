# 订阅管理后端运行时依赖（fnOS python312 / 本地开发）
# 安装: pip install -r requirements.txt
#
# 说明: fnOS 设备通过 install_dep_apps=python312 提供解释器。
# Flask 生态在打包时由 tools/vendor_deps.py 交叉安装为 Linux cp312 轮子，
# 写入 app/backend/vendor/（不要在 NAS 上对系统 Python 执行 pip install）。
Flask>=3.0,<4.0
Flask-SQLAlchemy>=3.1,<4.0
Flask-Migrate>=4.0,<5.0

SQLAlchemy~=2.0.52
alembic~=1.19.1